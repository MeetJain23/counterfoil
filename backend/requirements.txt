# Counterfoil dependencies. Pinned, because the figures in the README are
# reproducible claims and "it worked on the version I had" is not reproducible.
#
# Deliberately short. The Gemini client is written against urllib rather than a
# vendor SDK, and the audit ledger is JSONL rather than a database, so neither
# pulls a dependency tree in for one HTTP call or one append.

# --- runtime ---
PyYAML==6.0.3                 # the policy file
fastapi==0.141.1              # read-only dashboard API
uvicorn==0.52.4               # ASGI server for it

# --- optional: only needed to record new model fixtures ---
# The committed fixtures in llm_fixtures/ mean the eval reproduces with neither
# of these installed and no API key at all. Gemini needs nothing beyond the
# standard library.
anthropic==1.0.0              # only if COUNTERFOIL_LLM_PROVIDER=anthropic

# --- development ---
pytest==9.0.3
httpx==0.28.1                 # required by fastapi's TestClient
